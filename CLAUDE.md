# プロジェクト概要

このプロジェクトは、政治団体「財政健全化を求める市民の会」代表・桑原由樹が、Instagram投稿の原稿（カルーセル用テキスト・キャプション・ハッシュタグ）を作成するための作業スペースです。

- 団体名: 財政健全化を求める市民の会
- 代表: 桑原由樹
- 公式サイト: https://www.cafsjapan.com/
- Instagramアカウント: https://www.instagram.com/cafs.japan/（2026年8月時点フォロワー約12人、月1〜2回・動画中心の投稿で伸び悩み中）

`note/` `X/` フォルダも用意されています。note/では記事を作成しており、Instagramのネタ元としても活用します。

## 発信方針（2026年8月確定）

- **フォーマット: カルーセル（複数枚スライド）**。Canvaで実際のデザインはユーザーが作成するため、このフォルダでは「スライドごとのテキスト原稿＋キャプション＋ハッシュタグ＋アイキャッチ/画像イメージのメモ」をMarkdownで用意する。
- **更新頻度の目標：週2回**
- **ネタ元: note/の記事を要約し、Instagram向けに再構成する**のを基本とする。note記事が無いテーマを扱う場合は、出典を明記した上で個別に事実確認する。
- 主なテーマ: 経済ニュース解説（特に財政・消費税・国債・社会保障・最低賃金など、団体の政策領域）

## カルーセルの基本構成（2026-08-19改訂、9枚目安）

伸びている情報系Instagramアカウントの手法（2026年時点のアルゴリズム傾向）を踏まえた構成。出典：[neworder「Instagramカルーセル完全攻略」](https://www.neworder.co.jp/2026/04/19/instagram_carousel_8ro10/)、[SAKIYOMI「インスタのデザインに映えは不要」](https://sns-sakiyomi.com/blog/tips/instagram-design/)、[S.Line「Instagramアルゴリズム2026」](https://s--line.co.jp/instagram-algorithm-2026-latest/)。

1. **表紙**：具体的な数字を入れたインパクトのある見出し（曖昧な問いかけだけより「◯◯円」「◯%」など数字を明示する方が保存率が高い）
2. **「分かること」スライド（NEW）**：この投稿で何が分かるかを3項目程度で予告する。読了・保存の動機付けになる
3. **事実提示**：何が起きたか（日付・発表元を明記）
4〜6. **解説**：なぜ問題か／データ・背景（1スライド1メッセージ、短文・大きい文字を意識）
7. **会の立場**：「2つの安心運動」（財政の安心・社会保障の安心）の観点からの見解
8. **保存版まとめ（NEW）**：チェックリスト形式でポイントを3〜4個再掲する「あとで見返せる」スライド。保存率対策として重要
9. **CTA・出典**：保存に加えて**「◯◯な友達に送ってください」など具体的な共有依頼**を入れる（2026年のアルゴリズムは保存・シェアを「いいね」の約3倍重視するため、フォロー訴求だけでなく名指しでのシェア依頼が効果的）

- スライド内の文章は短く・大きく：1スライド最大3〜4行、1文を長くしすぎない（Instagramは"パッと見て伝わる"を優先）。1スライド1メッセージを守り、詰め込みすぎるくらいならスライド数を9〜10枚まで増やしてよい。
- noteのような長文の論理展開はキャプションや複数スライドに分割して圧縮する。
- 文体・トーン・事実確認のルールは `note/CLAUDE.md` の「文体・トーン」節に準拠する（インパクト重視だが、数字の出典・時期は必ず明記、根拠のない因果断定はしない）。
- **キャプションの末尾に問いかけを入れ、コメントを誘発する**（例：「賛成ですか？反対ですか？コメントで教えてください」）。コメント・保存・シェアはアルゴリズム上「いいね」より重視される。
- **構造的な注意点**：カルーセル／静止画投稿はフォロワー以外への露出（発見タブ経由のリーチ）が少なく、非フォロワーへのリーチはリール（動画）が圧倒的に有利。フォロワーが少ない段階（〜数百人）では、カルーセルの質を上げるだけでなく、リール投稿も並行して検討する価値がある（別途相談・意思決定が必要な事項）。

## ファイル管理

- 投稿原稿は `Instagram/` フォルダ内にMarkdownファイル（`.md`）として保存する
- ファイル名: `YYYY-MM-DD-投稿タイトルのスラッグ.md`（例: `2026-08-18-shohizei-1percent-genzei.md`）
- ファイル冒頭に投稿用メタ情報をHTMLコメントで記載する（想定公開日、ネタ元note記事へのリンク、キャプション案、ハッシュタグ案、Canvaでのアイキャッチ/配色イメージ）

## 作業フロー

1. ネタ選定：note記事、または直近の経済ニュース（出典明記できるもの）からテーマを選ぶ
2. 構成案：カルーセルのスライド構成（7〜8枚）を箇条書きで整理
3. 原稿作成：スライドごとの文章＋キャプション＋ハッシュタグを作成（Markdownファイルとして保存）
4. レビュー：事実関係・出典・トーンを確認
5. **画像化：テキスト原稿だけでなく、実際に使える画像（PNG）まで用意する。** Canvaでの手動制作がこれまで投稿頻度低下の主因になっていたため、原稿だけの納品では不十分。
   - **2026-08-19、このPCにNode.js（LTS、winget経由）をインストール済み。** これによりClaude Designのキャンバスエディタ（`design`スキル、`seed-canvas.mjs`の実行にnode/bunが必要）が使えるようになった。カルーセル画像は基本的に`design`スキルで作成する（アートボード＝スライド1枚として8枚を1つのキャンバスにまとめ、Artifactとして公開）。
     - ユーザーはブラウザ上でテキスト編集・色変更などの微調整ができ、ツールバーからPNG/PDFをスライド単位でエクスポートできる（Canva不要）。
     - `design`スキルが使えない場合（node/bunが無い環境に戻った場合など）は、代替として通常のArtifact＋SVGのforeignObject経由でPNG化＋`downloads`ランタイム機能（`capabilities: {downloads: true}`）でダウンロードボタンを提供する方式でも代用可能。
   - **ブランド素材**：CAFSロゴ画像あり（黒円に白いメガフォンのマーク＋「CAFS」文字、背景透過PNG。`Instagram/CAFS ロゴ.png`が高解像度の円形版、`Instagram/スクリーンショット 2025-07-19 140946.png`が団体フルネーム入りの横長バナー版）。カルーセルの各スライドに配置する際は、白背景バッジで囲んだり回転させたりせず、透過のまま等倍角度でそのまま置く（ユーザーからのフィードバックで確定：白い縁取りは不要、傾けない）。
   - **見出し用フォント**：Google Fontsの「Zen Kaku Gothic New」Black(900)をこの投稿の使用文字だけに絞ったサブセットで埋め込み、大きい数字・見出しに使うと訴求力が上がる（`https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:wght@900&text=<使用文字>`でサブセット版woff2を取得し、base64化して`@font-face`にdata URIとして埋め込む。本文は引き続きシステムフォント（Hiragino Sans/Yu Gothic UI/Noto Sans JP）でよい）。
   - デザインのアクセント：ロゴのメガフォンが斜めに傾いているモチーフを踏襲し、キッカーラベルの塗りつぶし、上部の斜めカットのアクセントバーなど「動きのある」要素を意識すると、フラットな配色だけより訴求力が出る（ただしロゴ自体は水平のまま）。
   - イラスト：8枚中2〜3枚に、内容に合った簡単なフラットイラスト（アイコン・チャートなど、写真は不使用）を入れると単調さが和らぐ。ブランドの色使い・ストローク幅に合わせる。
   - **画面上部の安全マージン**：Instagram（特にデスクトップ版web）は投稿画像の上部が見切れて表示されることがある（プロフィールグリッドは正方形センタークロップ、投稿詳細もウィンドウ高さによってはクロップされる場合がある）。ロゴバッジは画面最上部ギリギリ（top:30〜40px程度）ではなく、`top:96px`程度まで下げて配置し、上部の斜めアクセントバー以外の重要要素（ロゴ・タグ）が見切れないようにする。
   - 参考実装：`shohizei-1percent-genzei-carousel`（1本目）、`primary-balance-kurojika-carousel`（2本目）。いずれも2026-08-19、design スキルで作成、Artifactとして公開。
6. Canva制作（任意）：微調整したい場合のみ、書き出したPNGをCanvaに読み込んで使う
7. 画像書き出し：`Instagram/<フォルダ名>/`（例: `260819`）にPNG 9枚＋`caption.txt`（投稿原稿の「キャプション案」＋「ハッシュタグ案」を結合したもの）を保存する。
   - **2026-08-29以降、PNG書き出しはユーザーの手動エクスポート（Artifactのツールバー）を待たず、Claude自身がヘッドレスChromeで自動生成できる。** 各スライドの`.dc.html`ソース（フォント base64 埋め込み・ロゴPNG同梱済みのもの。design スキルでArtifactに publish する前の、seed-canvas.mjs に渡す作業ファイル）が入ったディレクトリで、`"C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --hide-scrollbars --window-size=1080,1350 --screenshot="<出力先>.png" "file:///<Windows形式の絶対パス>/SlideNN.dc.html"` を実行すると、1080×1350のPNGがそのまま得られる（`.dc.html`はカスタム要素`<x-dc>`/`<helmet>`で包まれているが、ブラウザは未知の要素を無視して中身の`<div class="slide">`をそのまま描画するため、`support.js`が無くても静的スライドは正しくレンダリングされる）。Bashツールでは`dangerouslyDisableSandbox: true`を付けないとスクリーンショットの書き込みが権限エラーになる点、`file://`のパスは`$(pwd)`のPOSIX形式ではなくWindows形式（`C:/Users/...`）で組み立てる必要がある点に注意。これにより「Canvaでの手動制作が投稿頻度低下の主因」という問題が完全に解消し、原稿作成から画像9枚生成までを人手を介さず一気通貫でできるようになった。
   - Artifactでの公開・ユーザーによるブラウザ上の微調整（テキスト編集など）が必要な場合は引き続きArtifactのツールバーからのPNG/PDFエクスポートも使える（並存する2通りの方法）。
8. Instagramへの投稿：手動（Instagramアプリでカルーセルとしてアップロード）、または下記の自動投稿の仕組みを使う。

## Instagramへの自動投稿（2026-08-19構築 → 同日、ローカルスクリプト方式に変更）

**重要：GitHub Actions（クラウド）経由の投稿は動作しない。** 最初はnote/のWix連携と同じ構成（別リポジトリ`cafsjapan-instagram`の`.github/workflows/instagram-publish.yml`からGitHub Actions経由でInstagram Graph APIを呼ぶ）を構築したが、**Meta側がGitHub Actionsのようなデータセンター/クラウドサーバーのIPアドレスからのPOSTリクエスト（投稿系の書き込み操作）を拒否する**ことが判明した（`graph.instagram.com`への同一トークン・同一パラメータのリクエストが、自宅PCからは成功し、GitHub Actionsのランナーからは常に`Invalid OAuth 2.0 Access Token`エラーで失敗した。ヘッダー・ボディ形式・User-Agent等を色々変えても症状は変わらなかった）。ワークフローファイル自体はリポジトリに残しているが、**現状は使えない**。

**実際に使う方式：`Instagram/publish-instagram.ps1`（自宅PCから直接実行）**

- 画像は引き続きGitHubリポジトリ（`cafsjapan-instagram`、公開/publicである必要あり）にpushし、raw.githubusercontent.com経由のURLとしてInstagram Graph APIに渡す（Graph APIは画像URLを要求するため、直接ファイルアップロードは不可）。
- ただし実際にAPIを呼び出す処理（トークンでの認証・投稿）は、**ユーザー自身のPC上でPowerShellスクリプトとして実行する**。自宅の一般的なインターネット回線のIPアドレスからのリクエストは正常に成功する。
- 使い方：`Instagram`フォルダで `.\publish-instagram.ps1 -Folder 260819` のように実行すると、①該当フォルダをgit push→②アクセストークンの入力を促す→③カルーセルの画像コンテナ作成→④公開、まで自動で行う。
- **アクセストークンは毎回、developers.facebook.comの「トークンを生成」で新しく発行してから貼り付ける運用**（長期トークンへの交換は試みたが確立できなかった。短期トークンでも即座に使えば十分）。
- `IG_USER_ID`は`28982078078050899`（Facebook Graph API側で見えるID`17841452838964183`とは別物。`graph.instagram.com`用にはApp-Scoped User IDである前者を使う）。
- Instagramアカウント連携のセットアップ（ビジネスアカウント化→Facebookページ/ビジネスポートフォリオ連携→Meta for Developersでアプリ作成→Instagramユースケース追加→Instagramテスターとしてユーザーネームを登録→承認→アクセストークン発行）はMeta側の作業のためユーザー本人が行う必要がある。特に「Instagramテスター」の役割は、Facebookアカウント名ではなくInstagramのユーザーネームで登録する点がハマりやすい。詳細手順は会話履歴を参照。
- 今後の改善余地：長期トークン化、自宅PC常時起動を前提としたself-hosted runner化、スマホからのトリガー方法の検討など。
